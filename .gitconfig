[color]
    ui = auto
[user]
    name = $GIT_COMMITTER_NAME
    email = $GIT_COMMITER_EMAIL
[github]
    user = $GITHUB_USERNAME
    token = $GITHUB_TOKEN
[color]
    ui = auto
[core]
    editor = vim
    autocrlf = input
[merge]
	conflictstyle = diff3
[alias]
    todo = grep --ignore-case -e ' FIX: *' -e ' TODO: *'
    ass = !echo 'What kind of git do you think I am?'
